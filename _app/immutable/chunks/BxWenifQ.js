import{a,f as l}from"./CU_Tu6Mf.js";import"./BiabzJe8.js";import{n as p}from"./DwHbYvjF.js";const c={title:"Hack Minesweeper - part 2",date:"2020-06-06",tags:["Hack Game"],description:"Hướng dẫn đọc bộ nhớ game Minesweeper bằng C# và hiển thị map bom ra console.",published:!0},{title:r,date:o,tags:m,description:u,published:g}=c;var e=l(`<h1>Hack Minesweeper - part 2</h1> <p>Nếu chưa xem phần 1 thì có thể xem lại tại <a href="https://uongsuadaubung.blogspot.com/2020/06/hack-minesweeper-part-1.html" rel="nofollow">đây</a>.
Mình sẽ sử dụng C# để code, mình cũng có đọc ở đâu đó trên vài trang nước ngoài thấy nhiều người nói là để viết mấy cái tool hack này nọ thì nên viết bằng C++ và mình cũng có tìm hiểu qua C++ nhưng thấy nó khó vãi, hoặc do mình tiếp xúc với C# nhiều hơn và sớm hơn nên quen hơn. Nói sơ sơ qua thì công việc cũng chỉ là đọc ghi giá trị từ địa chỉ ô nhớ trong game thui.</p> <p><img src="/images/hack-minesweeper-part-2/image-01.png" alt="Giao diện Minesweeper"/></p> <h2>Đọc bộ nhớ game</h2> <p>Đầu tiên chúng ta phải khai báo vài cái hàm API của Win32. Nếu ai chưa biết mấy hàm này thì có thể tìm hiểu thêm về DllImport nhé.</p> <div class="code-block"><pre><code class="hljs language-csharp">[<span class="hljs-meta">DllImport(<span class="hljs-string">&quot;kernel32.dll&quot;</span>)</span>]
<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">extern</span> IntPtr <span class="hljs-title">OpenProcess</span>(<span class="hljs-params"><span class="hljs-built_in">int</span> dwDesiredAccess, <span class="hljs-built_in">bool</span> bInheritHandle, <span class="hljs-built_in">int</span> dwProcessId</span>)</span>;
[<span class="hljs-meta">DllImport(<span class="hljs-string">&quot;kernel32.dll&quot;</span>)</span>]
<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">extern</span> <span class="hljs-built_in">bool</span> <span class="hljs-title">ReadProcessMemory</span>(<span class="hljs-params"><span class="hljs-built_in">int</span> hProcess, <span class="hljs-built_in">int</span> lpBaseAddress, <span class="hljs-built_in">byte</span>[] lpBuffer, <span class="hljs-built_in">int</span> dwSize, <span class="hljs-keyword">ref</span> <span class="hljs-built_in">int</span> lpNumberOfBytesRead</span>)</span>;</code></pre></div> <p>Khi một tiến trình được mở, ta phải chỉ định quyền truy cập mong muốn (với game Minesweeper thì mình chỉ muốn đọc giá trị thui nên yêu cầu quyền truy cập để đọc bộ nhớ), vì vậy hằng số này là cần thiết:</p> <div class="code-block"><pre><code class="hljs language-csharp"><span class="hljs-keyword">const</span> <span class="hljs-built_in">int</span> PROCESS_VM_READ = <span class="hljs-number">0x0010</span>;</code></pre></div> <p>Giới thiệu thêm:</p> <div class="code-block"><pre><code class="hljs language-csharp"><span class="hljs-keyword">const</span> <span class="hljs-built_in">int</span> PROCESS_VM_WRITE = <span class="hljs-number">0x0020</span>; <span class="hljs-comment">// yêu cầu quyền truy cập để ghi vào bộ nhớ</span>
<span class="hljs-keyword">const</span> <span class="hljs-built_in">int</span> PROCESS_ALL_ACCESS = <span class="hljs-number">0x1F0FFF</span>; <span class="hljs-comment">// đọc và ghi</span></code></pre></div> <h2>Ghi vào bộ nhớ game</h2> <div class="code-block"><pre><code class="hljs language-csharp">[<span class="hljs-meta">DllImport(<span class="hljs-string">&quot;kernel32.dll&quot;</span>)</span>]
<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">extern</span> IntPtr <span class="hljs-title">OpenProcess</span>(<span class="hljs-params"><span class="hljs-built_in">int</span> dwDesiredAccess, <span class="hljs-built_in">bool</span> bInheritHandle, <span class="hljs-built_in">int</span> ProcessId</span>)</span>;
[<span class="hljs-meta">DllImport(<span class="hljs-string">&quot;kernel32.dll&quot;</span>, SetLastError = true)</span>]
<span class="hljs-function"><span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">extern</span> <span class="hljs-built_in">bool</span> <span class="hljs-title">WriteProcessMemory</span>(<span class="hljs-params"><span class="hljs-built_in">int</span> hProcess, <span class="hljs-built_in">int</span> lpBaseAddress, <span class="hljs-built_in">byte</span>[] lpBuffer, <span class="hljs-built_in">int</span> dwSize, <span class="hljs-keyword">out</span> <span class="hljs-built_in">int</span> lpNumberOfBytesWritten</span>)</span>;</code></pre></div> <p>Một chương trình có cả đọc và viết thì dùng OpenProcess một lần là được.</p> <h2>Bắt tay vào code thôi!!!</h2> <p>Theo lý thuyết bên trên thì ban đầu phải import cái hàm đọc bộ nhớ vào, bài này mình chỉ đọc bộ nhớ thôi sang bài sau với game khác mình sẽ làm đủ cả đọc ghi.</p> <p><img src="/images/hack-minesweeper-part-2/image-02.png" alt="Khai báo import"/></p> <p>Đầu tiên phải khai báo một số biến hay ho ra trước, gồm mấy cái địa chỉ mà ở phần 1 mình tìm được:</p> <div class="code-block"><pre><code class="hljs language-csharp"><span class="hljs-built_in">int</span> widthAddr = <span class="hljs-number">0x010056AC</span>; <span class="hljs-comment">// chiều rộng map</span>
<span class="hljs-built_in">int</span> heightAddr = <span class="hljs-number">0x010056A8</span>; <span class="hljs-comment">// chiều cao map</span>
<span class="hljs-built_in">int</span> firstAddr = <span class="hljs-number">0x01005361</span>; <span class="hljs-comment">// ô mìn đầu tiên map[0][0]</span>
<span class="hljs-built_in">int</span> opendAddr = <span class="hljs-number">0x010057A4</span>; <span class="hljs-comment">// số ô đã mở</span>
<span class="hljs-built_in">byte</span>[] buffer = <span class="hljs-keyword">new</span> <span class="hljs-built_in">byte</span>[<span class="hljs-number">4</span>]; <span class="hljs-comment">// buffer là cái mà hàm yêu cầu, để nó lưu giá trị đọc được từ bộ nhớ vào buffer này, byte[4] là do tất cả các giá trị tìm được có kiểu lớn nhất 4bytes</span>
<span class="hljs-built_in">int</span> mywidth = <span class="hljs-number">0</span>, myheight = <span class="hljs-number">0</span>, myopend = <span class="hljs-number">0</span>; <span class="hljs-comment">// đọc được giá trị ra thì phải lưu vào để sử dụng</span>
<span class="hljs-built_in">int</span> mycurr = <span class="hljs-number">0</span>;
<span class="hljs-built_in">int</span> reads = <span class="hljs-number">0</span>; <span class="hljs-comment">// cái này cũng do hàm yêu cầu thôi, tạo cho đủ</span></code></pre></div> <p>Tiếp theo để mở tiến trình thì làm như sau:</p> <div class="code-block"><pre><code class="hljs language-csharp">Process process = Process.GetProcessesByName(<span class="hljs-string">&quot;Winmine__XP&quot;</span>)[<span class="hljs-number">0</span>];
IntPtr processHandle = OpenProcess(PROCESS_WM_READ, <span class="hljs-literal">false</span>, process.Id);</code></pre></div> <p>Cả đống nó sau thì sẽ trông thế này:</p> <p><img src="/images/hack-minesweeper-part-2/image-03.png" alt="Khai báo biến"/></p> <p>Phân tích tiếp, mình sẽ đọc giá trị liên tục để update lại lên màn hình, cho nên sẽ sử dụng một vòng lặp như sau:</p> <p><img src="/images/hack-minesweeper-part-2/image-04.png" alt="Vòng lặp chính"/></p> <p>Từ đây đọc các giá trị chiều rộng, chiều cao, và số ô đã mở đầu tiên:</p> <div class="code-block"><pre><code class="hljs language-csharp">ReadProcessMemory(processHandle, widthAddr, buffer, <span class="hljs-number">1</span>, <span class="hljs-keyword">ref</span> reads);
mywidth = BitConverter.ToInt32(buffer, <span class="hljs-number">0</span>);
ReadProcessMemory(processHandle, heightAddr, buffer, <span class="hljs-number">1</span>, <span class="hljs-keyword">ref</span> reads);
myheight = BitConverter.ToInt32(buffer, <span class="hljs-number">0</span>);
ReadProcessMemory(processHandle, opendAddr, buffer, <span class="hljs-number">1</span>, <span class="hljs-keyword">ref</span> reads);
myopend = BitConverter.ToInt32(buffer, <span class="hljs-number">0</span>);</code></pre></div> <p>Tại sao đọc số ô đã mở thì là do sau khi mình ấn mở ô đầu tiên xong thì game nó mới tạo map, cho nên là phát click đầu tiên không bao giờ dẫm phải bom đâu. Đến đây thì suy luận xíu là đc, nếu chưa mở ô nào thì vẽ cái map mặc định toàn dấu <code>-</code> (gạch ngang), còn nếu mở rồi thì mới đọc giá trị của map để vẽ ra ô nào là bom ô nào không.</p> <div class="code-block"><pre><code class="hljs language-csharp"><span class="hljs-keyword">if</span> (myopend == <span class="hljs-number">0</span>) <span class="hljs-comment">// số ô đã mở == 0 tức là chưa mở ô nào</span>
&#123;
    <span class="hljs-keyword">for</span> (<span class="hljs-built_in">int</span> i = <span class="hljs-number">0</span>; i &lt; myheight; i++)
    &#123;
        <span class="hljs-keyword">for</span> (<span class="hljs-built_in">int</span> j = <span class="hljs-number">0</span>; j &lt; mywidth; j++)
        &#123;
            Console.Write(<span class="hljs-string">&quot;- &quot;</span>);
        &#125;
        Console.WriteLine();
    &#125;
&#125;
<span class="hljs-keyword">else</span> <span class="hljs-comment">// ngược lại thì là đã mở</span>
&#123;
    <span class="hljs-comment"><span class="hljs-doctag">///</span>/ sẽ phân tích tiếp bên dưới</span>
&#125;</code></pre></div> <p>Đến đây chạy thử sẽ có kết quả thế này:</p> <p><img src="/images/hack-minesweeper-part-2/image-05.png" alt="Kết quả chưa mở ô"/></p> <p><img src="/images/hack-minesweeper-part-2/image-06.png" alt="Kết quả sau khi mở ô"/></p> <p>Trông có vẻ khá chính xác 😅😅</p> <p>Còn trong trường hợp là đã mở một ô và map được tạo thì sao? Mở CE lên và vào đó tìm hiểu một xíu:</p> <p><img src="/images/hack-minesweeper-part-2/image-07.png" alt="CE xem vùng nhớ"/></p> <p>Ấn Ctrl+B để mở xem vùng nhớ tại địa chỉ đó. Đặt vài cái cờ sẽ thấy giá trị của mấy địa chỉ liền kề nhau thay đổi, suy ra là với hàng ngang thì mỗi ô nó cách nhau 1 byte thôi, nên để đọc giá trị ô bên cạnh thì lấy địa chỉ hiện tại <strong>+1</strong> là được.</p> <p><img src="/images/hack-minesweeper-part-2/image-08.png" alt="Kiểm tra hàng ngang"/></p> <p>Thế còn khi hết hàng ngang và xuống hàng mới?</p> <p><img src="/images/hack-minesweeper-part-2/image-09.png" alt="Kiểm tra xuống hàng"/></p> <p>Nhìn ta thấy thì nó cách nhau một khoảng cố định là <strong>0x20</strong>, không tin thì lấy máy tính bấm giá trị sau trừ đi giá trị trước là ra.</p> <p><img src="/images/hack-minesweeper-part-2/image-10.png" alt="Tính khoảng cách"/></p> <p>Vậy để di chuyển đến dòng thứ <code>i</code> thì mình có công thức thế này <code>firstAddr + 0x20 * i</code>, ví dụ để nhảy đến dòng 5: <code>0x01005361 + 0x20*5 = 01005401</code>.</p> <p><img src="/images/hack-minesweeper-part-2/image-11.png" alt="Kiểm tra công thức"/></p> <p>Đó, địa chỉ có thật nhé, đúng công thức luôn. À ý mình là hàng thứ 6 nếu là trong game, cái hàng đầu tiên coi là hàng 0, nếu bạn có học về mảng rồi thì sẽ hiểu.</p> <p>Oke, đến đây bắt tay code nốt đoạn còn lại:</p> <div class="code-block"><pre><code class="hljs language-csharp"><span class="hljs-built_in">int</span> current = firstAddr;
<span class="hljs-keyword">for</span> (<span class="hljs-built_in">int</span> i = <span class="hljs-number">0</span>; i &lt; myheight; i++)
&#123;
    <span class="hljs-keyword">for</span> (<span class="hljs-built_in">int</span> j = <span class="hljs-number">0</span>; j &lt; mywidth; j++)
    &#123;
        ReadProcessMemory(processHandle, current, buffer, <span class="hljs-number">1</span>, <span class="hljs-keyword">ref</span> reads);
        mycurr = BitConverter.ToInt32(buffer, <span class="hljs-number">0</span>);
        <span class="hljs-keyword">if</span> (mycurr == <span class="hljs-number">0x8F</span> || mycurr == <span class="hljs-number">0x8E</span> || mycurr == <span class="hljs-number">0x8A</span>) <span class="hljs-comment">// 0x8F là bom, 0x8E là đặt cờ, 0x8A là bom mở ra sau khi kết thúc game</span>
        &#123;
            Console.Write(<span class="hljs-string">&quot;x &quot;</span>);
        &#125;
        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mycurr == <span class="hljs-number">0xCC</span>) <span class="hljs-comment">// 0xCC là quả bom lúc ấn vào nó nổ</span>
        &#123;
            Console.Write(<span class="hljs-string">&quot;0 &quot;</span>);
        &#125;
        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (mycurr == <span class="hljs-number">0x0F</span>) <span class="hljs-comment">// 0x0F là ô mặc định chưa mở hoặc chưa đặt cờ</span>
        &#123;
            Console.Write(<span class="hljs-string">&quot;- &quot;</span>);
        &#125;
        <span class="hljs-keyword">else</span>
        &#123;
            Console.Write(<span class="hljs-string">&quot;  &quot;</span>);
        &#125;
        current++; <span class="hljs-comment">// đi đến ô tiếp theo</span>
    &#125;
    current = (firstAddr + <span class="hljs-number">0x20</span> * (i + <span class="hljs-number">1</span>)); <span class="hljs-comment">// 0x20 là giá trị mỗi hàng cách nhau</span>
    Console.WriteLine();
&#125;</code></pre></div> <p>Và đây là thành quả đạt được:</p> <iframe title="video" allowfullscreen="allowfullscreen" frameborder="0" height="500" src="https://www.blogger.com/video.g?token=AD6v5dxvxh7AJb5Rzuc9rAPgfmjFtp6py17I-LWYFwhh_IQkZGLCQIPojELMYJjTiBkQTFpqbsiGmQwpsDpHeZlrle-dznWNYn8-5fgQp98t2OjsdRDc_Wh-Oro1cApctn4YOfHPsVY&amp;origin=uongsuadaubung.blogspot.com" width="500"></iframe> <p>Full code mình sẽ update tại <a href="https://github.com/uongsuadaubung/Hack-Minesweeper-csharp" rel="nofollow">đây</a>.</p> <p>Điều này trái ngược với tư duy logic của game đề ra, nhưng thui dù sao mình cũng tư duy theo cách của mình mà nhỉ, học hỏi thêm được cái khác hay ho hơn là ngồi đếm số thui đúng không :V</p>`,1);function d(s){var n=e();p(96),a(s,n)}export{d as default,c as metadata};
