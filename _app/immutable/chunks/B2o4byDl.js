import{a,f as t}from"./CU_Tu6Mf.js";import"./BiabzJe8.js";import{n as l}from"./DwHbYvjF.js";const h={title:"Tự code tool tải tài liệu Slideshare và đóng gói thành Web App",date:"2025-02-19",tags:["Lập Trình","Tool & Extension","Trải Nghiệm"],description:"Hành trình từ một script chống cháy tải PDF giúp người nhà, cho đến lúc vã mồ hôi hột đóng gói thành một trang Web hoàn chỉnh chạy đa luồng.",published:!0},{title:o,date:r,tags:g,description:m,published:d}=h;var c=t(`<h1>Tự code tool tải tài liệu Slideshare và đóng gói thành Web App</h1> <p>Chuyện là hôm nọ, đang mổ cò hì hục fix bug thì tự dưng bà chị gái nhắn tin nhờ tải hộ cái tài liệu trên Slideshare. Chị bảo lướt thấy hay quá mà lại ngại đăng ký tài khoản lằng nhằng, nhờ mình tải hộ file PDF về xem cho tiện. Mình lẩm bẩm trong miệng đoạn lên Google gõ ‘Slideshare downloader’ xem có rổ web tự động nào không.</p> <p>Tìm đại một công cụ tải trực tuyến rồi dán link vào, nhưng trong lúc ngồi đợi mòn mỏi server của họ tải file, cơn tò mò nghề nghiệp nổi lên. Mình bật tính năng F12 lên xem các công cụ online này sử dụng mánh khóe gì để bóc tách tệp.</p> <p>Ngồi khảo sát cấu trúc DOM của trang tải, sau đó lượn sang đọc DOM gốc của trang Slideshare thì mình chợt nhận ra… đáp án nằm ngay trước mắt!</p> <h2>1. Phát hiện cơ chế và Script đầu tay</h2> <p>Hóa ra thay vì bóc tách API ngầm quá cao siêu, hệ thống Slideshare xử lý mặt tiền khá đơn giản: họ nén nguyên tất cả các link ảnh của từng slide vào ngay bên trong một thẻ <code>div</code> bí mật mang ID <code>#new-player</code>. Không những thế, ảnh còn được chia sẵn ra 3 kích thước để lựa chọn: <code>320w</code>, <code>638w</code>, và rõ nét <code>2048w</code>.</p> <p>Các công cụ downloader trực tuyến thực tế chỉ làm nghiệp vụ cạo dữ liệu DOM (scrape) để gom toàn bộ danh sách link ảnh này lại, tải về và nén lại thành 1 file PDF nguyên xi.</p> <p>Thấy quy trình này quá ư là dễ đoán, mình mở ngay con IDE lên, viết một đoạn Script bằng Puppeteer + Typescript với tư duy thực dụng để lách luật. Khởi động Puppeteer chạy headless, trỏ đến URL của tài liệu, chờ thẻ <code>#new-player</code> xuất hiện rồi bắt đầu dùng Regex chọc lấy hết các đuôi phân giải <code>2048w</code>.</p> <div class="code-block"><pre><code class="hljs language-typescript"><span class="hljs-comment">// Trích xuất đoạn mã chứa link</span>
<span class="hljs-keyword">const</span> htmlContent = <span class="hljs-keyword">await</span> page.$eval(<span class="hljs-string">&#x27;#new-player&#x27;</span>, <span class="hljs-function"><span class="hljs-params">el</span> =&gt;</span> el.<span class="hljs-property">innerHTML</span>?.<span class="hljs-title function_">trim</span>());

<span class="hljs-comment">// Quăng regex vô tìm toàn bộ link jpg siêu nét</span>
<span class="hljs-keyword">const</span> regex = <span class="hljs-keyword">new</span> <span class="hljs-title class_">RegExp</span>(<span class="hljs-string">\`https?:\\\\/\\\\/[^\\\\s&quot;]+\\\\.jpg(?=\\\\s<span class="hljs-subst">$&#123;job.size&#125;</span>w)\`</span>, <span class="hljs-string">&quot;g&quot;</span>);
<span class="hljs-keyword">const</span> <span class="hljs-attr">matches</span>: <span class="hljs-built_in">string</span>[] = htmlContent.<span class="hljs-title function_">match</span>(regex) || [];</code></pre></div> <p>Sau khi có link ảnh, mình lôi thư viện <code>pdf-lib</code> và <code>sharp</code> ra để sắp xếp ảnh thành một trang PDF. Nhồi buffer ảnh vào trang PDF rồi lưu lại thế là xong, vứt file ném trả bà chị. Quá đơn giản… nếu câu chuyện chỉ dừng lại ở đây.</p> <h2>2. Nỗi ám ảnh “Giao diện người dùng”</h2> <p>Code xong script chạy terminal rẹt rẹt ra file, mình tự dưng thấy ngứa nghề. Công cụ hay thế này mà khóa chặt trong màn hình dòng lệnh thì hơi phí, ngoài mình ra chẳng ai xài được. Mình nảy số tự hỏi: “Tại sao không bọc nó lại thành một trang web giao diện thân thiện nhỉ?“. Thế là mình quyết định chơi lớn: <strong>Nâng cấp trực tiếp nó thành một Web App thực thụ</strong> để cộng đồng mạng hay bất kỳ ai có link đều dễ dàng dán vào tải được luôn.</p> <p>Mình dựng vội cái server Express, dùng EJS làm giao diện nhập URL, và kết nối MongoDB để lưu trạng thái tải file của từng user. Cứ tưởng ném cái hàm tạo PDF hôm qua vào trong Router của Express là xong bài. Mình hý hửng gõ lệnh chạy thử, mở 2 tab lên test tính năng tải song song.</p> <p>BÙM. Màn hình console đỏ lừ, tab trình duyệt quay mòng mòng, server giật lag không phản hồi.</p> <p>Ngồi debug mờ cả mắt, mãi lúc sau mới nhảy số phát hiện ra vấn đề oái oăm: Quá trình dùng <code>sharp</code> để convert Buffer ảnh và <code>pdf-lib</code> để vẽ lại file PDF là một <strong>tác vụ ngốn CPU cực tàn bạo!</strong> Vì Node.js vốn chạy đơn luồng (Single Thread), nên khi luồng chính đang gồng mình xử lý tạo PDF cho tab thứ nhất, nó block luôn toàn bộ luồng request, làm tab thứ hai và tất cả người dùng khác kẹt cứng không truy cập được server.</p> <h2>3. Quá trình vật lộn với Worker Threads</h2> <p>Nhận ra lỗi sai tai hại về thiết kế hệ thống, mình tức tốc lao vào tái cấu trúc. Bài toán giờ đây là phải đưa toàn bộ khâu tải ảnh và nhào nặn PDF ra một luồng riêng. Lời giải xuất hiện: <code>worker_threads</code>.</p> <p>Mình ném đoạn code tạo PDF sang một file độc lập tên là <code>pdfWorker.ts</code>. Tuy nhiên, nếu có 100 người vào nhập link cùng lúc mà gọi ra 100 Worker thì chắc xẹp luôn cả con máy chủ RAM 1GB của mình. Mình bắt buộc phải code thêm một lớp <strong>WorkerPool</strong> ngầm để giới hạn số luồng hoạt động.</p> <p>Đây là đoạn code xương máu đã ra đời trong khoảnh khắc bế tắc đó:</p> <div class="code-block"><pre><code class="hljs language-typescript"><span class="hljs-keyword">import</span> &#123; <span class="hljs-title class_">Worker</span> &#125; <span class="hljs-keyword">from</span> <span class="hljs-string">&#x27;worker_threads&#x27;</span>;
<span class="hljs-comment">// ...</span>

<span class="hljs-keyword">class</span> <span class="hljs-title class_">WorkerPool</span> &#123;
  <span class="hljs-keyword">private</span> <span class="hljs-attr">pool</span>: <span class="hljs-title class_">Worker</span>[] = [];
  <span class="hljs-keyword">private</span> <span class="hljs-keyword">readonly</span> <span class="hljs-attr">maxWorkers</span>: <span class="hljs-built_in">number</span> = <span class="hljs-number">5</span>; <span class="hljs-comment">// Chỉ cho phép chạy tối đa 5 luồng tải PDF</span>
  
  <span class="hljs-keyword">async</span> <span class="hljs-title function_">createPdf</span>(<span class="hljs-params"><span class="hljs-attr">imageUrls</span>: <span class="hljs-built_in">string</span>[], <span class="hljs-attr">job</span>: <span class="hljs-title class_">DownloadItemModel</span></span>) &#123;
    <span class="hljs-comment">// Nếu số luồng đang bận chạm ngưỡng, bắt request đợi nghỉ một lát</span>
    <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">pool</span>.<span class="hljs-property">length</span> &gt;= <span class="hljs-variable language_">this</span>.<span class="hljs-property">maxWorkers</span>) &#123;
      <span class="hljs-keyword">await</span> <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">waitForAvailableWorker</span>();
    &#125;
    
    <span class="hljs-comment">// Khởi tạo luồng thợ phụ</span>
    <span class="hljs-keyword">const</span> workerPath = path.<span class="hljs-title function_">resolve</span>(__dirname, <span class="hljs-string">&quot;../worker/pdfWorker.js&quot;</span>);
    <span class="hljs-keyword">const</span> worker = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Worker</span>(workerPath, &#123;
      <span class="hljs-attr">workerData</span>: &#123; job, imageUrls &#125;,
    &#125;);
    
    <span class="hljs-variable language_">this</span>.<span class="hljs-property">pool</span>.<span class="hljs-title function_">push</span>(worker);
    
    worker.<span class="hljs-title function_">on</span>(<span class="hljs-string">&quot;exit&quot;</span>, <span class="hljs-function">(<span class="hljs-params">code</span>) =&gt;</span> &#123;
      <span class="hljs-comment">// Chạy xong, đá nó ra khỏi Pool để nhường chỗ cho request khác</span>
      <span class="hljs-variable language_">this</span>.<span class="hljs-property">pool</span> = <span class="hljs-variable language_">this</span>.<span class="hljs-property">pool</span>.<span class="hljs-title function_">filter</span>(<span class="hljs-function"><span class="hljs-params">w</span> =&gt;</span> w !== worker);
    &#125;);
  &#125;
  
  <span class="hljs-keyword">private</span> <span class="hljs-title function_">waitForAvailableWorker</span>(): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">void</span>&gt; &#123;
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function"><span class="hljs-params">resolve</span> =&gt;</span> &#123;
      <span class="hljs-keyword">const</span> interval = <span class="hljs-built_in">setInterval</span>(<span class="hljs-function">() =&gt;</span> &#123; <span class="hljs-comment">// Tái kiểm tra mỗi 100ms</span>
        <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">pool</span>.<span class="hljs-property">length</span> &lt; <span class="hljs-variable language_">this</span>.<span class="hljs-property">maxWorkers</span>) &#123;
          <span class="hljs-built_in">clearInterval</span>(interval);
          <span class="hljs-title function_">resolve</span>();
        &#125;
      &#125;, <span class="hljs-number">100</span>);
    &#125;);
  &#125;
&#125;</code></pre></div> <p>Mỗi User gửi request xong, server lập tức đẩy dữ liệu ID xuống MongoDB và đưa công việc vào hàng chờ của WorkerPool. Người dùng chỉ việc nhìn trang Web hiển thị thông báo “Đang xử lý PDF…” lặp lại sau mỗi 2 giây, trong khi ở dưới hầm máy rầm rập chạy. Xong xuôi, database cập nhật <code>isDone = true</code> và quăng nút tải về cho người dùng nhấn.</p> <h2>4. Chốt sổ</h2> <p>Gửi luôn một cái link web public sang cho bà chị dùng, thấy bả thao tác mượt mà tải file sầm sập mà lòng vui như trúng số. Thật ra cái hành trình từ một dòng Script ăn xổi, đắp thịt dần qua mô hình bất đồng bộ và tối ưu đa luồng bằng Worker là thứ tốn không ít nơ-ron thần kinh, nhưng cái cảm giác thấy server mình chịu tải trơn tru xứng đáng đến từng phút đồng hồ mày mò.</p> <p>Tất cả code dự án Server + Worker này, anh em nào tò mò cách setup Express bắt tay với Puppeteer và MongoDB thì cứ sang repo Github tài khoản của mình mà tham khảo nhé. Cứ clone về, điền URI MongoDB vô là chạy phe phé. Hành trình cày cuốc công cụ cào mạng này xin khép lại, hẹn anh em ở một bài vật lộn hệ thống kế tiếp :v!</p>`,1);function u(n){var s=c();l(46),a(n,s)}export{u as default,h as metadata};
