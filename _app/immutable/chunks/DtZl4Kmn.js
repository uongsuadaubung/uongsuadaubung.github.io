import{a,f as t}from"./CU_Tu6Mf.js";import"./BiabzJe8.js";import{n as c}from"./DwHbYvjF.js";const h={title:"Tự code thiệp cưới xịn xò: Trận chiến Google Apps Script và Deploy chia 2 họ",date:"2026-02-24",tags:["Lập Trình","Trải Nghiệm","Chuyện Nghề"],description:"Cứ tưởng tự viết cái thiệp cưới ném lên mạng là nhanh gọn lẹ, ai ngờ đâm đầu vào mới thấy muôn vàn rắc rối từ vụ vượt rào CORS của Google đến bài toán tách Web nhà trai - nhà gái.",published:!0},{title:e,date:r,tags:g,description:d,published:m}=h;var l=t(`<h1>Tự code thiệp cưới xịn xò: Trận chiến Google Apps Script và Deploy chia 2 họ</h1> <p>Chuyện là ngày trọng đại chuẩn bị đến, vợ chồng mình ngồi lướt chán chê các mẫu thiệp cưới số trên mạng. Nhìn thì đẹp thật đấy, nhưng dùng mấy nền tảng dựng sẵn (builder) cứ có cảm giác rập khuôn thế nào ấy, lại còn hay dính quảng cáo hay giới hạn băng thông.</p> <p>Vốn tính dân IT không thích xài hàng có sẵn, mình vỗ ngực dõng dạc bảo vợ: “Ba cái web HTML tĩnh này để anh múa phím một đêm là xong, cần gì phải mua!“. Nhưng đúng là câu nói đi trước, cái bước lết theo sau. Bắt tay vào làm mới thấy một mớ góc khuất mà chỉ lúc cạy code ra mới thấu được.</p> <h2>1. Nỗi đau lưu trữ dữ liệu với Google Apps Script (GAS)</h2> <p>Làm trang tĩnh (Static site) hiển thị thông tin rạp cưới, ảnh cô dâu chú rể thì quá dễ. Nhưng cái thiệp cưới thời 4.0 bắt buộc phải có mục “Gửi lời chúc” (Wish Wall) để anh em bạn bè điền form lưu lại. Vấn đề là: Web tĩnh thì lấy đâu ra Database để lưu? Mua hẳn một con máy chủ hay xài Firebase chỉ để host thiệp cưới xài vài tuần thì nghe quá… phí tiền.</p> <p>Thế là mình nảy số: Tại sao không tận dụng Google Sheets làm Database, và dùng <strong>Google Apps Script (GAS)</strong> làm “API nội bộ”? Mình hỳ hục viết một đoạn script trên Google Drive nhận request POST để ghi thẳng lời chúc vào file Excel.</p> <p>Hý hửng vào trang web, nhập lời chúc mẫu, bấm GỬI… BÙM! Trình duyệt báo đỏ lừ: <code>CORS Policy Error</code>. Mọi nỗ lực gọi hàm <code>fetch</code> bình thường bằng <code>application/json</code> đều bị Google chặn đứng từ vòng gửi xe (Preflight Request). Bế tắc, lên mạng rảo bước hết các diễn đàn, cuối cùng mình cũng mò ra được một “thủ thuật hắc ám”: Chuyển đổi Content-Type sang dạng thô để đánh lừa trình duyệt!</p> <p>Dưới đây là tuyệt chiêu xử lý AJAX “vượt rào” đẫm nước mắt của mình:</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-comment">// Gửi dữ liệu dưới dạng text/plain để lách Preflight check của CORS</span>
$.<span class="hljs-title function_">ajax</span>(&#123;
    <span class="hljs-attr">url</span>: <span class="hljs-variable constant_">API_URL</span>,
    <span class="hljs-attr">method</span>: <span class="hljs-string">&#x27;POST&#x27;</span>,
    <span class="hljs-attr">data</span>: <span class="hljs-title class_">JSON</span>.<span class="hljs-title function_">stringify</span>(&#123; name, message &#125;),
    <span class="hljs-attr">contentType</span>: <span class="hljs-string">&#x27;text/plain; charset=utf-8&#x27;</span>, 
    <span class="hljs-attr">success</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params">response</span>) &#123;
        <span class="hljs-title class_">UIkit</span>.<span class="hljs-title function_">notification</span>(&#123; <span class="hljs-attr">message</span>: <span class="hljs-string">&#x27;Đã gửi lời chúc! ❤️&#x27;</span>, <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;success&#x27;</span> &#125;);
        <span class="hljs-built_in">setTimeout</span>(loadWishes, <span class="hljs-number">1500</span>);
    &#125;,
    <span class="hljs-attr">error</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params">err</span>) &#123;
        <span class="hljs-comment">// Nỗi đau thứ 2: GAS ghi dữ liệu thành công nhưng lại văng ra Redirect 302.</span>
        <span class="hljs-comment">// Trình duyệt coi đó là lỗi. Kệ nó, mình cứ quất luôn thông báo Success!</span>
        <span class="hljs-title class_">UIkit</span>.<span class="hljs-title function_">notification</span>(&#123; <span class="hljs-attr">message</span>: <span class="hljs-string">&#x27;Cảm ơn bạn đã gửi lời chúc! ❤️&#x27;</span>, <span class="hljs-attr">status</span>: <span class="hljs-string">&#x27;success&#x27;</span> &#125;);
        <span class="hljs-built_in">setTimeout</span>(loadWishes, <span class="hljs-number">1500</span>);
    &#125;
&#125;);</code></pre></div> <p>Hóa ra lúc chạy xong lệnh ghi dữ liệu, Google Apps Script luôn tự tạt một cái Redirect 302 chứ không thèm trả về mã 200 JSON cho đàng hoàng. Mình đành cắn răng đưa logic thành công vào luôn cả nhánh <code>error</code>. Cốt sao dữ liệu vào được bảng Excel là ăn mừng!</p> <h2>2. Bài toán nhức não: Một Source Code, Hai Nhà Gái - Trai</h2> <p>Tưởng chừng xong xuôi, vợ mình lại dặn: <em>“Thiệp bên nhà gái phải để tên nhà gái trước, stk của em, bản đồ nhà em. Thiệp nhà trai phải stk của anh, bản đồ nhà anh nhé!”</em>.</p> <p>Nếu làm theo tư duy thông thường, mình có thể tạo một đống câu lệnh IF ELSE vướng víu trong mã nguồn, dựa vào URL có param <code>?side=nhatrai</code> để chuyển giao diện. Nhưng như thế cái đuôi URL gửi cho người lớn rất xấu. Hoặc copy ra thành 2 project riêng biệt? Lúc code lại hay sửa ảnh thì lại phải chép qua chép lại à? KHÔNG BAO GIỜ! Tổ nghề không cho phép mình lặp lại mã (Don’t Repeat Yourself).</p> <p>Để giữ cho thư mục làm việc luôn chỉ là 1 source code gốc duy nhất, mình quyết định viết một file thần thánh <code>deploy.js</code> ở tầng Build bằng NodeJS. File script này sẽ thay mình tự động đóng gói toàn bộ và đập thẳng lên <strong>2 repository hoàn toàn khác nhau</strong> trên Github.</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-keyword">const</span> folders = [<span class="hljs-string">&#x27;groom&#x27;</span>, <span class="hljs-string">&#x27;bride&#x27;</span>];
<span class="hljs-keyword">const</span> folderConfig = &#123;
    <span class="hljs-string">&#x27;groom&#x27;</span>: <span class="hljs-string">&#x27;https://github.com/uongsuadaubung/nha-trai.git&#x27;</span>,
    <span class="hljs-string">&#x27;bride&#x27;</span>: <span class="hljs-string">&#x27;https://github.com/uongsuadaubung/nha-gai.git&#x27;</span>,
&#125;;

<span class="hljs-keyword">async</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">deploy</span>(<span class="hljs-params"></span>) &#123;
    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> folder <span class="hljs-keyword">of</span> folders) &#123;
        <span class="hljs-keyword">const</span> folderPath = path.<span class="hljs-title function_">join</span>(buildDir, folder);
        <span class="hljs-keyword">const</span> remoteUrl = folderConfig[folder];

        <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">\`\\n--- Vận công Deploy: <span class="hljs-subst">$&#123;folder.toUpperCase()&#125;</span> ---\`</span>);
        
        <span class="hljs-comment">// Tuyệt kĩ: Đập bỏ thư mục git cũ để commit đè lên như mới khởi tạo</span>
        <span class="hljs-keyword">if</span> (<span class="hljs-keyword">await</span> fs.<span class="hljs-title function_">pathExists</span>(path.<span class="hljs-title function_">join</span>(folderPath, <span class="hljs-string">&#x27;.git&#x27;</span>))) &#123;
            <span class="hljs-keyword">await</span> fs.<span class="hljs-title function_">remove</span>(path.<span class="hljs-title function_">join</span>(folderPath, <span class="hljs-string">&#x27;.git&#x27;</span>));
        &#125;

        <span class="hljs-keyword">const</span> commands = [
            <span class="hljs-string">&#x27;git init -b main&#x27;</span>,
            <span class="hljs-string">&#x27;git config user.name &quot;uongsuadaubung&quot;&#x27;</span>,
            <span class="hljs-string">\`git remote add origin <span class="hljs-subst">$&#123;remoteUrl&#125;</span>\`</span>,
            <span class="hljs-string">&#x27;git add .&#x27;</span>,
            <span class="hljs-string">\`git commit -m &quot;Deploy <span class="hljs-subst">$&#123;folder&#125;</span> - Lên đồ!&quot;\`</span>,
            <span class="hljs-string">&#x27;git push -f origin main&#x27;</span> <span class="hljs-comment">// Ép văng hết dữ liệu repos cũ</span>
        ];

        <span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> cmd <span class="hljs-keyword">of</span> commands) &#123;
            <span class="hljs-title function_">execSync</span>(cmd, &#123; <span class="hljs-attr">cwd</span>: folderPath, <span class="hljs-attr">stdio</span>: <span class="hljs-string">&#x27;inherit&#x27;</span> &#125;);
        &#125;
    &#125;
&#125;</code></pre></div> <p>Bây giờ mỗi lúc cần cập nhật thiết kế hay đổi ảnh cưới, mình chỉ việc gõ đúng một lệnh <code>npm run deploy</code> trên Terminal. Node.js tự động build ra folder <code>groom</code> và <code>bride</code>, cài riêng các biến môi trường cấu hình, chạy <code>git init</code> tạo kho mới tại chỗ và <code>git push -f</code> dập thẳng lên Github Pages. Chạy rẹt rẹt cực sướng, đố cần phân vân copy - paste nhầm thư mục nữa!</p> <h2>3. Tổng kết</h2> <p>Mặc dù chỉ là chiếc thiệp cưới được chắp vá trong vỏn vẹn vài buổi tối bớt xén giờ nghỉ ngơi, nhưng cảm giác đem những thứ tự code ra ứng dụng thực vào đời sống thực tế sướng rần rần.</p> <p>Thế mới thấy, công nghệ sinh ra cốt yếu là để giải quyết các vấn đề thực tiễn của chính bản thân. Bạn chuẩn bị tổ chức đám cưới, hay muốn làm một trang sự kiện nhanh gọn không dính Database cồng kềnh, cứ mạnh dạn vào Github kéo bộ code <code>my-wedding</code> của mình về và tham khảo nhé. Chúc anh em fix lỗi mướt mát!</p>`,1);function u(s){var n=l();c(36),a(s,n)}export{u as default,h as metadata};
