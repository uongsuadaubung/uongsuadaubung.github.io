import{a,f as t}from"./DdKyNRsu.js";import"./2eH-Kdi_.js";import{n as c}from"./D64QkPoJ.js";const h={title:"Tự viết Chrome Extension dọn dẹp Newsfeed Facebook",date:"2021-10-23",tags:["Lập Trình","Tool & Extension","Trải Nghiệm"],description:"Chán cảnh mở Facebook lên toàn rác, gợi ý linh tinh với quảng cáo, mình quyết định tự code một chiếc extension để dọn dẹp, nhưng thực tế gian truân hơn mình tưởng nhiều.",published:!0},{title:o,date:r,tags:m,description:g,published:u}=h;var l=t(`<h1>Tự viết Chrome Extension dọn dẹp Newsfeed Facebook</h1> <p>Những năm gần đây mình bắt đầu có một nỗi sợ nhè nhẹ mỗi khi gõ vòng vòng trên thanh địa chỉ chữ <code>facebook.com</code>. Mở màn hình lên, thay vì được xem cập nhật của bạn bè người thân, đập vào mặt mình chỉ rặt một mớ “Gợi ý cho bạn”, “Sponsor”, rồi thì ba cái Reels xàm xí từ những trang không quen biết.</p> <p>Rõ ràng thuật toán của Facebook ngày càng giống một cái máy nhồi nhét nội dung thập cẩm, còn mình thì chỉ muốn một không gian mạng mộc mạc và sạch sẽ. Cơn giận tích tụ làm mình lóe lên ý tưởng: “Tại sao không tự viết một cái Chrome Extension thu dọn đống rác này?“. Hý hửng mở IDE lên, cứ đinh ninh chỉ cần vài dòng Regex là nhẹ gánh, ai ngờ đâu Facebook lại chuẩn bị sẵn đủ thứ oái oăm để hành hạ mấy gã táy máy biểu thức DOM như mình.</p> <h2>1. Hành trình đi dọn rác cơ bản</h2> <p>Khảo sát nhanh bộ mặt DOM của Facebook, mình phát hiện mọi thứ được render bằng React, và các bài đăng trên Newsfeed đều để lại dấu vết ở thuộc tính <code>data-pagelet^="FeedUnit_"</code>. Tư duy ban đầu rất đơn giản, mình lập một danh sách các từ khóa ám tiễn:</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-keyword">let</span> stringAds = [
    <span class="hljs-string">&quot;Post you may like&quot;</span>,
    <span class="hljs-string">&quot;Suggested for you&quot;</span>,
    <span class="hljs-string">&quot;Sponsored&quot;</span>,
    <span class="hljs-string">&quot;Videos just for you&quot;</span>,
    <span class="hljs-string">&quot;People You May Know&quot;</span>,
    <span class="hljs-string">&quot;Gợi ý cho bạn&quot;</span>
]</code></pre></div> <p>Xong chỉ việc vác vòng lặp đi tìm các phần tử, chém sạch những thằng nào chứa từ khóa. Để tối ưu tài nguyên rà quét, mình khéo léo gắn thêm một class <code>done</code> để đánh dấu những phần tử đã duyệt qua.</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-keyword">let</span> notScanned = <span class="hljs-variable language_">document</span>.<span class="hljs-title function_">querySelectorAll</span>(<span class="hljs-string">&#x27;div:not(.done)[data-pagelet^=&quot;FeedUnit_&quot;]&#x27;</span>);
<span class="hljs-keyword">for</span> (<span class="hljs-keyword">const</span> ele <span class="hljs-keyword">of</span> notScanned) &#123;
    ele.<span class="hljs-property">classList</span>.<span class="hljs-title function_">add</span>(<span class="hljs-string">&quot;done&quot;</span>);
    <span class="hljs-keyword">if</span> (stringAds.<span class="hljs-title function_">some</span>(<span class="hljs-function"><span class="hljs-params">ads</span> =&gt;</span> ele.<span class="hljs-property">innerText</span>.<span class="hljs-title function_">includes</span>(ads)))&#123;
        ele.<span class="hljs-title function_">remove</span>() <span class="hljs-comment">// Tiễn vong</span>
        <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;Đã xoá quảng cáo: &quot;</span>, ele.<span class="hljs-property">innerText</span>.<span class="hljs-title function_">slice</span>(<span class="hljs-number">0</span>, <span class="hljs-number">20</span>))
    &#125;
&#125;</code></pre></div> <p>Mọi thứ có vẻ rất hoàn hảo vào lúc ban đầu, cho đến khi mình lướt qua thẻ Watch…</p> <h2>2. Nỗi ám ảnh trùng lặp Video và Hàm băm cyrb53</h2> <p>Mạng xã hội hiển thị cơ chế nhồi nhét bài viết quá tàn nhẫn, khi mình cuộn chuột trong phần <code>/watch</code>, thuật toán liên tục tải lại các đoạn video mình vừa mới lướt qua không lâu trước đó. Cảm giác vô cùng ức chế vì nội dung nghèo nàn.</p> <p>Để giải quyết, mình phải đi sâu vào nội dung của từng cấu trúc Video card (<code>div.j83agx80.cbu4d94t</code>). Bài toán đặt ra là: Làm sao để máy tính nhận diện được đâu là video đã xem rồi mà không làm đơ trình duyệt?</p> <p>Ngồi debug mờ cả mắt, cuối cùng mình quyết định sử dụng giải pháp băm chuỗi nội dung văn bản (Hash) của từng post thành một con số duy nhất, bằng giải thuật băm <strong>cyrb53</strong> trứ danh.</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-comment">// Hàm băm nội dung để tạo con dấu riêng biệt cho mỗi bài đăng</span>
<span class="hljs-keyword">function</span> <span class="hljs-title function_">cyrb53</span> (<span class="hljs-params">str, seed = <span class="hljs-number">0</span></span>) &#123;
    <span class="hljs-keyword">let</span> h1 = <span class="hljs-number">0xdeadbeef</span> ^ seed, h2 = <span class="hljs-number">0x41c6ce57</span> ^ seed;
    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>, ch; i &lt; str.<span class="hljs-property">length</span>; i++) &#123;
        ch = str.<span class="hljs-title function_">charCodeAt</span>(i);
        h1 = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h1 ^ ch, <span class="hljs-number">2654435761</span>);
        h2 = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h2 ^ ch, <span class="hljs-number">1597334677</span>);
    &#125;
    h1 = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h1 ^ (h1&gt;&gt;&gt;<span class="hljs-number">16</span>), <span class="hljs-number">2246822507</span>) ^ <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h2 ^ (h2&gt;&gt;&gt;<span class="hljs-number">13</span>), <span class="hljs-number">3266489909</span>);
    h2 = <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h2 ^ (h2&gt;&gt;&gt;<span class="hljs-number">16</span>), <span class="hljs-number">2246822507</span>) ^ <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">imul</span>(h1 ^ (h1&gt;&gt;&gt;<span class="hljs-number">13</span>), <span class="hljs-number">3266489909</span>);
    <span class="hljs-keyword">return</span> <span class="hljs-number">4294967296</span> * (<span class="hljs-number">2097151</span> &amp; h2) + (h1&gt;&gt;&gt;<span class="hljs-number">0</span>);
&#125;

<span class="hljs-comment">// Bên trong vòng quét</span>
<span class="hljs-keyword">let</span> split = ele.<span class="hljs-property">innerText</span>.<span class="hljs-title function_">split</span>(<span class="hljs-string">&#x27;\\n&#x27;</span>);
<span class="hljs-comment">// Chỉ lấy phần chữ làm dữ kiện để tạo hash</span>
<span class="hljs-keyword">let</span> hash = <span class="hljs-title function_">cyrb53</span>(split.<span class="hljs-title function_">slice</span>(<span class="hljs-number">0</span>, split.<span class="hljs-property">length</span> - <span class="hljs-number">10</span>).<span class="hljs-title function_">join</span>());

<span class="hljs-keyword">if</span> (!loadedPost.<span class="hljs-title function_">includes</span>(hash)) &#123;
    loadedPost.<span class="hljs-title function_">push</span>(hash);
&#125; <span class="hljs-keyword">else</span> &#123;
    <span class="hljs-comment">// Tìm thấy mã băm trùng lặp, tự động gỡ bài đăng</span>
    ele.<span class="hljs-title function_">remove</span>();
&#125;</code></pre></div> <p>Trải nghiệm quả thực cực kỳ lôi cuốn! Trình duyệt giờ mượt mà lọc từng mã bài trùng lặp bay màu trong tích tắc.</p> <h2>3. Khắc tinh Single Page Application (SPA): Cuộc chiến giành bảo mật Chat</h2> <p>Một trong những động lực viết Extension của mình là có tính năng ngụy trang danh sách tên liên hệ ở thanh cột phải <code>RightRail</code>. Nhiều khi đi làm, mình ghét cảm giác ai đó đi qua ngước thẳng vào nhìn xem mình đang nhắn tin với ai. Ý tưởng ban đầu chỉ là cắt vụn dãy tên đó, rồi hoán đổi vị trí đảo lộn (random) để nguỵ trang, nghe rất xịn xò:</p> <div class="code-block"><pre><code class="hljs language-javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">encryptName</span>(<span class="hljs-params">name</span>) &#123;
    <span class="hljs-keyword">return</span> name.<span class="hljs-title function_">split</span>(<span class="hljs-string">&#x27;&#x27;</span>).<span class="hljs-title function_">sort</span>(<span class="hljs-function">() =&gt;</span> <span class="hljs-number">0.5</span> - <span class="hljs-title class_">Math</span>.<span class="hljs-title function_">random</span>()).<span class="hljs-title function_">join</span>(<span class="hljs-string">&#x27;&#x27;</span>);
&#125;</code></pre></div> <p>Mình hý hửng chèn trực tiếp dòng đó để thay tên người liên hệ, ấn Save, Reload web. Mọi tên đều được đảo lộn. Tưởng thế là đã thành công mỹ mãn…</p> <p>Nhưng cuộc đời không tha cho dân coder. Sau nháy mắt có người mới online, React phía nền móng của Facebook quét luồng Re-render (tải lại dữ liệu DOM không qua làm mới trang). Tất cả các tên bị đảo lộn mà mình khổ nhọc tạo ra tự nhiên bật thẳng về trạng thái tên tĩnh gốc ban đầu. Facebook React đã nghiền nát luồng ép tên ảo của mình! Mình gục ngã hoàn toàn với mớ hỗn độn này khi cố gắng lắng nghe bắt sự kiện biến thiên (Mutation Observer) mà vẫn chậm hơn tốc độ Virtual DOM của họ.</p> <p>Cuối cùng, sau những giờ phút bế tắc đỏ cả màn Console, mình đành áp dụng giải pháp “trâu cày” nhất nhưng hiệu quả nhất: <strong>Lên lịch ép ngược thời gian siêu nhạy</strong>. Mình tạo một hàm setInterval đếm 1ms để đè lập tức lại tên đã mã hoá, liên tục ép chết hiển thị:</p> <div class="code-block"><pre><code class="hljs language-javascript">nodeDiv.<span class="hljs-property">backupName</span> ??= name.<span class="hljs-property">innerText</span>
nodeDiv.<span class="hljs-property">encrypt</span> ??= <span class="hljs-title function_">encryptName</span>(nodeDiv.<span class="hljs-property">backupName</span>)

name.<span class="hljs-property">innerText</span> = nodeDiv.<span class="hljs-property">encrypt</span> <span class="hljs-comment">// đổi ngay thành tên lạ</span>

<span class="hljs-comment">// Đoạn code vật lộn đẻ ra setInterval rào dữ liệu SPA</span>
<span class="hljs-keyword">let</span> ms = <span class="hljs-number">1</span>; 
name.<span class="hljs-property">clearcheck</span> ??= <span class="hljs-built_in">setInterval</span>(repeatcheck, ms);
<span class="hljs-keyword">let</span> time = <span class="hljs-number">0</span>;

<span class="hljs-keyword">function</span> <span class="hljs-title function_">repeatcheck</span>(<span class="hljs-params"></span>) &#123;
    <span class="hljs-keyword">if</span> (name.<span class="hljs-property">innerText</span> !== nodeDiv.<span class="hljs-property">encrypt</span>) &#123;
        <span class="hljs-comment">// Hễ React có nỗ lực đổi về chữ cũ, mình đập lại tên mã hoá</span>
        name.<span class="hljs-property">innerText</span> = nodeDiv.<span class="hljs-property">encrypt</span>;
        <span class="hljs-built_in">clearInterval</span>(name.<span class="hljs-property">clearcheck</span>); <span class="hljs-comment">// giải phóng tiến trình</span>
        
        time += ms;
        <span class="hljs-comment">// Chạy tối đa maxInterval rồi nghỉ để tránh treo Browser</span>
        <span class="hljs-keyword">if</span> (time &gt; maxInterval) &#123;
            <span class="hljs-built_in">clearInterval</span>(name.<span class="hljs-property">clearcheck</span>);
        &#125;
    &#125;
&#125;</code></pre></div> <p>Ngoài ra mình còn cạp thêm 2 EventListener lắng nghe <code>mouseover</code> và <code>mouseout</code> để trả về tên thật mỗi lúc mình chủ động trỏ chuột vào dòng liên hệ ấy.</p> <h2>4. Tổng Kết</h2> <p>Được tận mắt quan sát thành quả: một chiếc màn hình sạch sẽ, không quảng cáo, mất biệt luôn Video trùng lặp và tên chat loạn cào cào mã hóa, đây cảm giác thoả mãn cực độ. Nhưng đổi lại là bao nhiêu phen trầm ngâm, bế tắc debug các dòng biến số, rồi mới thấu hiểu sự chặt chẽ khó lường của hệ thống Single Page Applications hiện đại.</p> <p>Mảnh ghép kỹ năng học được qua trải nghiệm đổ mồ hôi này chắc chắn xứng đáng hơn nhiều việc đọc sách. Bạn nào đang tìm kiếm giải pháp cho đôi mắt và muốn vọc vạch sâu hơn tư duy phân tích DOM, mời tạc sang kho github của mình tại repo <code>facebook-ext</code> kéo về dùng và tự nghịch thiết lập nhé. Hẹn chia sẻ ở bài viết tốn calo khác!</p>`,1);function d(n){var s=l();c(50),a(n,s)}export{d as default,h as metadata};
