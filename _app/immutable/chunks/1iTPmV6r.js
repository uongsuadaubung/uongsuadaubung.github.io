import{a,f as t}from"./CU_Tu6Mf.js";import"./BiabzJe8.js";import{n as l}from"./DwHbYvjF.js";const i={title:"Làm cái app chat P2P với WebRTC và Svelte 5",date:"2026-04-18",tags:["WebRTC","Svelte 5","P2P","Chat"],description:"Chia sẻ về cách mình làm một cái app chat trực tiếp giữa hai trình duyệt, không cần server lưu tin nhắn, dùng Svelte 5 cho mượt.",published:!0},{title:o,date:r,tags:m,description:g,published:u}=i;var h=t(`<p><img src="/images/chat-p2p/hero.png" alt="App Chat P2P"/></p> <p>Chào mọi người, dạo này mình có ngồi vọc làm một cái app chat P2P (kiểu chat trực tiếp giữa 2 máy với nhau mà không qua server trung gian). Ý tưởng là để tin nhắn riêng tư hơn, không ai đọc trộm được vì nó chẳng lưu ở đâu ngoài máy mình và máy người kia.</p> <p>Tiện thể mình dùng luôn Svelte 5 xem nó có gì hay ho. Sau đây là mấy cái mình thấy đáng chú ý trong lúc làm.</p> <h3>1. Gửi tin nhắn sao cho không bị lỗi (Atomic Transactions)</h3> <p>Trong môi trường chat trực tiếp (WebRTC), mạng đôi khi nó chập chờn. Nếu bạn đang sửa tin nhắn hay thả icon mà mạng rớt phát là coi như máy mình một kiểu, máy kia một kiểu.</p> <p>Để giải quyết, mình làm một cái bộ quản lý hành động. Khi bạn bấm nút:</p> <ol><li>Máy sẽ lưu lại trạng thái cũ.</li> <li>Hiện kết quả lên màn hình luôn cho nhanh (Optimistic).</li> <li>Lưu vào một cái “hàng chờ” ở máy mình (IndexedDB).</li> <li>Gửi đi. Nếu gửi lỗi, nó sẽ tự động trả lại trạng thái cũ và đợi lúc nào có mạng thì tự gửi lại sau.</li></ol> <div class="code-block"><pre><code class="hljs language-typescript"><span class="hljs-comment">// Ví dụ đoạn code xử lý trả lại trạng thái cũ nếu lỗi</span>
<span class="hljs-keyword">private</span> <span class="hljs-title function_">rollbackUIState</span>(<span class="hljs-attr">action</span>: <span class="hljs-title class_">AtomicAction</span>, <span class="hljs-attr">originalState</span>: <span class="hljs-title class_">OriginalState</span>): <span class="hljs-built_in">void</span> &#123;
    <span class="hljs-keyword">const</span> m = ctx.<span class="hljs-property">messages</span>;
    <span class="hljs-keyword">const</span> roomMsgs = m[action.<span class="hljs-property">roomId</span>] || [];
    <span class="hljs-keyword">const</span> idx = roomMsgs.<span class="hljs-title function_">findIndex</span>(<span class="hljs-function">(<span class="hljs-params">m</span>) =&gt;</span> m.<span class="hljs-property">id</span> === action.<span class="hljs-property">messageId</span>);

    <span class="hljs-keyword">if</span> (idx === -<span class="hljs-number">1</span>) <span class="hljs-keyword">return</span>;

    <span class="hljs-keyword">const</span> updated = [...roomMsgs];
    <span class="hljs-comment">// Trả lại nội dung cũ nếu không gửi được</span>
    <span class="hljs-keyword">switch</span> (action.<span class="hljs-property">type</span>) &#123;
        <span class="hljs-keyword">case</span> <span class="hljs-string">&#x27;edit&#x27;</span>:
            updated[idx] = &#123; ...updated[idx], <span class="hljs-attr">text</span>: (originalState <span class="hljs-keyword">as</span> <span class="hljs-built_in">any</span>).<span class="hljs-property">text</span> &#125;;
            <span class="hljs-keyword">break</span>;
    &#125;
    ctx.<span class="hljs-property">messages</span> = &#123; ...m, [action.<span class="hljs-property">roomId</span>]: updated &#125;;
&#125;</code></pre></div> <h3>2. Gửi file lớn mà không bị đứng máy</h3> <p>Gửi file qua WebRTC nếu cứ tống cả cục vào là trình duyệt nó đơ luôn. Mình phải chia nhỏ file ra từng mẩu (chunk) tầm 16KB hay 32KB gì đó rồi gửi dần.</p> <p>Cái khó là phải biết lúc nào thì nên gửi tiếp, lúc nào nên dừng lại chờ bên kia nhận xong (Backpressure). Mình dùng cái hàm <code>onbufferedamountlow</code> của trình duyệt. Đại loại là khi nào bộ đệm của trình duyệt nó vơi đi thì mình mới nhồi thêm mẩu tiếp theo vào.</p> <div class="code-block"><pre><code class="hljs language-typescript"><span class="hljs-comment">// Chờ cho bộ đệm vơi bớt rồi mới gửi tiếp</span>
<span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">waitForBufferDrain</span>(<span class="hljs-params"><span class="hljs-attr">targetPeers</span>: <span class="hljs-title class_">Set</span>&lt;<span class="hljs-built_in">string</span>&gt;</span>): <span class="hljs-title class_">Promise</span>&lt;<span class="hljs-built_in">void</span>&gt; &#123;
  <span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function">(<span class="hljs-params">resolve</span>) =&gt;</span> &#123;
    ch.<span class="hljs-property">onbufferedamountlow</span> = <span class="hljs-function">() =&gt;</span> &#123;
      ch.<span class="hljs-property">onbufferedamountlow</span> = <span class="hljs-literal">null</span>;
      <span class="hljs-title function_">resolve</span>();
    &#125;;
    <span class="hljs-comment">// Đợi tối đa 0.5s nếu không thấy báo gì thì cũng cứ chạy tiếp</span>
    <span class="hljs-built_in">setTimeout</span>(resolve, <span class="hljs-number">500</span>);
  &#125;);
&#125;</code></pre></div> <h3>3. Svelte 5 dùng khá là sướng</h3> <p>Svelte 5 có mấy cái “Rune” mới như <code>$state</code> giúp quản lý dữ liệu rất nhàn. Mình tạo một cái file chứa toàn bộ dữ liệu chung của app, chỗ nào cần thì gọi ra dùng, dữ liệu thay đổi là giao diện tự cập nhật theo, không cần phải dùng mấy cái Store loằng ngoằng như bản cũ.</p> <h3>4. Nhấn F5 vẫn không mất tin nhắn</h3> <p>Vì không có server lưu tin nhắn nên mình dùng luôn cái database trong trình duyệt (IndexedDB) để lưu lịch sử.</p> <ul><li>Mỗi lần có tin nhắn mới là mình lưu vào máy luôn.</li> <li>Khi bạn nhấn F5 hoặc load lại trang, app sẽ đọc từ máy ra hiển thị lại.</li> <li>Khi kết nối lại với bạn bè, hai bên sẽ hỏi nhau: “Ông có thiếu tin nhắn nào không?” để gửi bù cho nhau.</li></ul> <h3>5. Một vài cái râu ria khác</h3> <p>Mình cũng làm thêm mấy cái hiệu ứng trong suốt (Glassmorphism) nhìn cho nó hiện đại tí, rồi cả gọi video, chia sẻ màn hình nữa. Nói chung là làm cái này học được khá nhiều về cách mạng hoạt động.</p> <h3>6. Demo giao diện</h3> <p>Đây là giao diện thực tế của app khi chạy:</p> <p><img src="/images/chat-p2p/demo.png" alt="Demo app chat"/></p> <h3>Trải nghiệm thử</h3> <p>Các bạn có thể vào đây để nghịch thử nhé: <a href="https://uongsuadaubung.github.io/chat/" rel="nofollow">https://uongsuadaubung.github.io/chat/</a></p> <p>Cảm ơn các bạn đã đọc bài chia sẻ ngắn gọn này của mình!</p>`,1);function d(n){var s=h();l(48),a(n,s)}export{d as default,i as metadata};
